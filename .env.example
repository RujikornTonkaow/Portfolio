# Backend API origin. The app resolves the current host with
# GET /api/v1/public/sites/by-domain?host=<host>
# and then loads /api/v1/public/sites/<siteId>/portfolio.
NUXT_PUBLIC_API_BASE_URL=http://localhost:8080
